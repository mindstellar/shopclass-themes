{
    "author": "Mindstellar",
    "author_uri": "https://github.com/mindstellar",
    "categories": [
        "general",
        "minimal"
    ],
    "description_html": "<h1>Folio</h1>\n<p>A minimal public theme for <a href=\"https://github.com/mindstellar/shopclass\" rel=\"nofollow noopener noreferrer\" target=\"_blank\">Shopclass</a>. Typography-first, no framework, no build step — and the reference implementation of the theme chrome contract.</p>\n<h2>The premise</h2>\n<p>Folio is built out of the elements the browser already ships. <code>&lt;dialog&gt;</code> is the seller-contact modal, <code>&lt;details&gt;</code>/<code>&lt;summary&gt;</code> are the collapsible filters, <code>&lt;search&gt;</code> wraps the search regions, <code>&lt;time&gt;</code> carries the machine-readable dates. Native form validation, <code>loading=&quot;lazy&quot;</code> and <code>autocomplete</code> hints do the rest. Light and dark come from <code>color-scheme</code> and <code>light-dark()</code>; RTL comes from logical properties, so there is no second stylesheet for either.</p>\n<p>What that buys:</p>\n<ul><li><strong>One stylesheet.</strong> <code>style.css</code>, ~26 KB unminified, hand-written, no preprocessor. Design tokens are CSS custom properties at the top of the file — change the palette or the type scale by editing them.</li><li><strong>Eight lines of JavaScript</strong>, inline in the footer, with no library behind them: a click delegate that opens a <code>&lt;dialog&gt;</code> in place. Every link it enhances is a real link first, so the page works with scripting off.</li><li><strong>No build.</strong> What is committed is what ships. Clone it, symlink it, edit it.</li></ul>\n<p>The theme consumes only the public theme API (<code>osc_*</code> helpers and hooks) and reaches into no core internals.</p>\n<h2>Translating</h2>\n<p>All user-visible strings go through the <code>folio</code> text domain. The catalogue is <code>languages/folio.pot</code>; drop a translated <code>languages/&lt;locale&gt;/theme.po</code> beside it.</p>\n<p>After changing any UI string, regenerate the catalogue:</p>\n<pre><code>node bin/i18n.mjs</code></pre>\n<p>Node&#039;s standard library only — no npm, no install step. The tool is not part of the release archive.</p>\n<h2>Requirements</h2>\n<ul><li>Shopclass 6.3.0 or newer</li><li>PHP 8.0 or newer</li></ul>\n<h2>Install</h2>\n<p>Download <code>folio_X.Y.Z.zip</code> from the <a href=\"https://github.com/mindstellar/theme-folio/releases\" rel=\"nofollow noopener noreferrer\" target=\"_blank\">releases</a>, unzip it into <code>oc-content/themes/</code> so you have <code>oc-content/themes/folio/</code>, and activate it under <strong>Settings → Appearance</strong>.</p>\n<p>For local development, clone next to your Shopclass checkout and symlink it in:</p>\n<pre><code>git clone git@github.com:mindstellar/theme-folio.git\nln -s &quot;$(pwd)/theme-folio&quot; /path/to/shopclass/oc-content/themes/folio</code></pre>\n<p><code>oc-content/themes/*</code> is gitignored in the core repo, so the symlink won&#039;t show up there.</p>\n<h2>Theme chrome</h2>\n<p>Some pages belong to core rather than to the theme. Core renders those inside the theme&#039;s own header and footer when the theme says where they are. Folio declares the pair outright in <code>functions.php</code>:</p>\n<pre><code>osc_add_theme_support(&#039;chrome&#039;, array(\n    &#039;header&#039; =&gt; &#039;common/header.php&#039;,\n    &#039;footer&#039; =&gt; &#039;common/footer.php&#039;,\n));</code></pre>\n<p>The contract is documented at <a href=\"https://mindstellar.com/docs/developers/theme-chrome/\" rel=\"nofollow noopener noreferrer\" target=\"_blank\">mindstellar.com/docs/developers/theme-chrome</a>. The declaration is guarded by <code>function_exists()</code>, so the theme still loads on 6.2 — where core&#039;s <code>common/header.php</code> + <code>common/footer.php</code> probe finds the same pair anyway.</p>\n<h2>Settings</h2>\n<p><strong>Requires Shopclass 6.3.0.</strong> Under <strong>Appearance → Folio</strong> an operator can set a logo, a brand colour, and a footer note. The page is declared with the settings API rather than hand-rolled, so core owns the form, the CSRF check and the save. On 6.2 the page does not appear and the theme falls back to its defaults, same as if nothing were ever set.</p>\n<h2>The account pages are core&#039;s</h2>\n<p>Folio <strong>ships no account or sign-in views at all</strong> — no <code>user-dashboard.php</code>, no <code>user-profile.php</code>, no <code>user-login.php</code>, none of the thirteen. Core draws them, between Folio&#039;s masthead and Folio&#039;s footer, and <code>style.css</code> restyles them through the class names core publishes for that purpose. There is no PHP involved on this theme&#039;s side.</p>\n<p>Those pages are not missing. Shipping stub copies of them would be thirteen files to keep in step with core for no gain. The vocabulary is documented at <a href=\"https://mindstellar.com/docs/developers/account-pages/\" rel=\"nofollow noopener noreferrer\" target=\"_blank\">mindstellar.com/docs/developers/account-pages</a>; the block that styles it is the last section of <code>style.css</code>.</p>\n<p>Adding one back is a matter of dropping the file in — the theme&#039;s view wins over core&#039;s, per page, with nothing to declare.</p>\n<h2>Plugin hooks</h2>\n<p>Folio fires the hook names existing plugins already target:</p>\n<table><thead><tr><th>Hook</th><th>Where</th></tr></thead><tbody><tr><td><code>header</code>, <code>footer</code></td><td><code>&lt;head&gt;</code> end / before <code>&lt;/body&gt;</code></td></tr><tr><td><code>item_detail</code>, <code>item_form</code></td><td>Item page body / post + edit form</td></tr><tr><td><code>item_contact_form</code>, <code>contact_form</code></td><td>Inside the seller-contact and site-contact forms</td></tr><tr><td><code>user_dashboard</code>, <code>user_alerts</code></td><td>Account pages (fired by core, inside this theme&#039;s chrome)</td></tr><tr><td><code>user_form</code>, <code>user_profile_form</code>, <code>user_register_form</code></td><td>Account and registration forms (same)</td></tr><tr><td><code>user_menu</code>, <code>user_menu_filter</code></td><td>The account nav — how a plugin adds an entry to it</td></tr></tbody></table>\n<h2>Translations</h2>\n<p>UI strings use the gettext domain <strong><code>folio</code></strong> (<code>__()</code> / <code>_e()</code>), which core loads from <code>languages/&lt;locale&gt;/theme.mo</code> for the active locale. No catalogue ships yet; the theme falls back to the English source strings.</p>\n<h2>Releases</h2>\n<p>Pushing a <code>vX.Y.Z</code> tag runs <code>.github/workflows/release.yml</code>, which packages the theme as <code>folio_X.Y.Z.zip</code> and publishes a GitHub release. There is nothing to build first.</p>\n<h2>License</h2>\n<p>GPL-3.0-or-later. © Navjot Tomer (Mindstellar) and contributors.</p>",
    "downloads": 65,
    "funding": null,
    "homepage": "https://github.com/mindstellar/theme-folio",
    "icon": "https://raw.githubusercontent.com/mindstellar/theme-folio/main/screenshot.png",
    "latest_version": "0.4.2",
    "license": "GPL-3.0-or-later",
    "name": "folio",
    "requires_min": "6.3.0",
    "screenshots": [],
    "short_description": "A typography-first Shopclass theme built on native HTML5 elements — one hand-written stylesheet, eight lines of JavaScript, no build step.",
    "slug": "folio",
    "source": "external",
    "support": {
        "docs": null,
        "issues": "https://github.com/mindstellar/theme-folio/issues"
    },
    "tags": [
        "native-html",
        "no-build",
        "no-javascript",
        "typography"
    ],
    "tested_max": "6.4",
    "type": "theme",
    "updated_at": "2026-09-26T04:51:07Z",
    "versions": [
        {
            "changelog_html": "",
            "downloads": 0,
            "published_at": "2026-09-26T04:51:07Z",
            "requires": "6.3.0",
            "requires_php": "8.0",
            "sha256": "b404c7981be22c4af851cf92e98e3bf579a63369ac4561e414a540f9bd464759",
            "size": 156481,
            "tested": "6.4",
            "url": "https://github.com/mindstellar/theme-folio/releases/download/v0.4.2/folio_0.4.2.zip",
            "version": "0.4.2"
        },
        {
            "changelog_html": "",
            "downloads": 25,
            "published_at": "2026-09-17T04:28:51Z",
            "requires": "6.3.0",
            "requires_php": "8.0",
            "sha256": "58cf62f41f25f808a8207230904d2729d9aa9086dc29fda5be63c7fbc59769f8",
            "size": 155982,
            "tested": "6.3",
            "url": "https://github.com/mindstellar/theme-folio/releases/download/v0.4.1/folio_0.4.1.zip",
            "version": "0.4.1"
        },
        {
            "changelog_html": "",
            "downloads": 18,
            "published_at": "2026-09-15T05:40:02Z",
            "requires": "6.3.0",
            "requires_php": "8.0",
            "sha256": "305deafa794b85711dbccb6f44cb5f2fb3760b0c97eecb02ce2f45edced400c4",
            "size": 155731,
            "tested": "6.3",
            "url": "https://github.com/mindstellar/theme-folio/releases/download/v0.4.0/folio_0.4.0.zip",
            "version": "0.4.0"
        },
        {
            "changelog_html": "",
            "downloads": 22,
            "published_at": "2026-09-04T09:29:58Z",
            "requires": "6.3.0",
            "requires_php": "8.0",
            "sha256": "0585f49f817bbbb0471bc7874b45a78655c9a62adcee43049940446b7e9a8dcc",
            "size": 152354,
            "tested": "6.3",
            "url": "https://github.com/mindstellar/theme-folio/releases/download/v0.1.0/folio_0.1.0.zip",
            "version": "0.1.0"
        }
    ]
}
